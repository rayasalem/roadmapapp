'use strict';
/**
 * Node (Render) only: "node expo/AppEntry.js" runs this file → start static server.
 * Web: Metro resolves to AppEntry.web.js instead, so this file is not bundled.
 */
require('./serverBootstrap.js');
